/**
 * PurrCrypt WebUI - Configuration
 *
 * Edit apiUrl to point to your PurrCrypt API.
 * The default "./api" works when the WebUI and API are served
 * from the same origin (e.g. via a reverse-proxy).
 */
const PURRCRYPT_CONFIG = {
  apiUrl: "./api",
};
